# Core (required for all backends)
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
scipy>=1.10.0

# LIVE backend (install separately — see LIVE documentation)
# diffvg

# StarVector backend
# transformers>=4.38.0
# accelerate>=0.27.0
# einops>=0.7.0
