"""LIVE (Layer-wise Image Vectorization) backend.

Requires the sidecar environment with diffvg and PyTorch installed.
Install via Settings → Enhanced Backends in the Re:Trace app.
"""


def trace(input_path: str, output_path: str, backend: str) -> None:
    # TODO (milestone 3): implement LIVE inference
    # from live_vectorization import vectorize
    # vectorize(input_path, output_path)
    raise NotImplementedError(
        "LIVE backend not yet available. "
        "Install the sidecar environment via Settings → Enhanced Backends."
    )
