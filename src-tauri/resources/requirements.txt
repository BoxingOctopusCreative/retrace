# Base ML environment — required for all enhanced backends (LIVE, StarVector).
# Installed by uv into an isolated venv managed by Re:Trace.
# Backend-specific deps (transformers, accelerate, etc.) are installed on-demand
# by download_model when the user installs a specific backend.
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
scipy>=1.10.0
